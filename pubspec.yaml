name: stream_subscriber
description: >-
  Observable objects for asynchronously listening for changes to values and
  collections, plus a mixin to easily manage streams on any class.
version: 0.1.0
homepage: https://github.com/james-alex/stream_subscriber

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  meta: ^1.1.8

dev_dependencies:
  test: ^1.15.4
  pedantic: ^1.9.0
